#include <stdio.h>

int main()
{
  int idade; // criei a variavel idade
  printf("Digite sua idade: "); // imprimi na tela a mensagem
  scanf("%d", &idade); // leia a idade digitada pelo usuario
  printf("sua idade eh: %d\n", idade); // imprima na tela a idade digitada pelo usuario
  return 0;
}
